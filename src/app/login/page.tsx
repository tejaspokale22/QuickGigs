"use client";

import Login from "@/components/Login";

const Page: React.FC = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
        <Login />
    </div>
  );
};

export default Page;
